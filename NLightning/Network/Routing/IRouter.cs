using System.Collections.Generic;
using NLightning.Network.Models;

namespace NLightning.Network.Routing
{
    public interface IRouter
    {
        //List<Route> GetRoutes(Channel sender, Channel receiver);
    }
}